/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

slope = true;

type = 1;

if(image_xscale > 0){
	
	type = image_yscale > 0 ? 1 : 3
	
}
else
{
	type = image_yscale > 0 ? 2 : 4
}